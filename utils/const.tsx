const {
   PROD_ENDPOINT : "",
   DEV_ENDPOINT : ""
} = process.env

const SERVICE_ORIGINS: {
    PROD_ENDPOINT: string;
    DEV_ENDPOINT: string;
  } = {
    DEV_ENDPOINT,
    DEV_ENDPOINT
   
  };
  export default SERVICE_ORIGINS;